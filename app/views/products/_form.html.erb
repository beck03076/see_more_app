<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :desc %><br />
    <%= f.text_area :desc %>
  </div>
   <div class="field">
    <%= f.label :color %><br />
    <%= f.text_field :color %>
  </div>
   <div class="field">
    <%= f.label :score %><br />
    <%= f.text_field :score %>
  </div>
  <div class="field">
    <%= f.label :assigned_to %><br />
    <%= f.collection_select :assigned_to, User.order(:email),:id,:email %>
  </div>  
   <div class="field">
    <%= f.label :product_status_id %><br />
    <%= f.collection_select :product_status_id, ProductStatus.order(:name),:id,:name %>
  </div>  
 <br/>
 <br/>
 
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
 <br/>
 <br/>
